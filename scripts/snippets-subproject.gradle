plugins {
    id("com.android.library")
    id("kotlin-android")
    id("kotlin-kapt")
    id("kotlinx-serialization")
    id("androidx.navigation.safeargs.kotlin")
}

apply(from: androidCommonSettings)
android {
    defaultConfig {
        manifestPlaceholders = [facebookAppId: ""]
    }
}

dependencies {
    // ...
    kapt(AndroidKaptDeps.core)
}